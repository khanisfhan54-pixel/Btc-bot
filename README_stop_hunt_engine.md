# Stop Hunt Engine
- `data/`: input schemas and stores.
- `features/`: feature engineering only.
- `model/`: classifier, regime routing, calibration, and inference engine.
- `validation/`: walk-forward and audits.
- `tests/`: contract checks for ordering, fallback behavior, calibration bounds, regime fallback, and leakage-free splits.
